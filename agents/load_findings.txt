HAVEN REPORT
Base URL: http://app:8000
Routes found: 22 routes

SECURITY RUN SUMMARY
- Contexts attempted: anon,userA,userB,admin
- Contexts authenticated (non-0 status): anon,userA,userB,admin
- Checks executed: missing-auth=YES priv-esc=YES idor=YES sensitive-leak=YES
- Requests made (if known): 57
- Access matrix (route outcomes counts):
  anon_2xx=5 anon_401_403=0 anon_other=0
  userA_2xx=5 userA_401_403=0 userA_other=1
  userB_2xx=5 userB_401_403=0 userB_other=1
  admin_2xx=5 admin_401_403=0 admin_other=1

SECURITY (security_boundary_scan)
- Confidence: HIGH
- Findings: CRIT 0 | HIGH 6 | MED 0 | LOW 0
- Top 5 risks (max 5 bullets, max 1 line each):
  1) Missing authentication on POST /projects — anon users can create projects without authentication (200 response)
  2) Missing authentication on POST /patients — anon users can create patient records without authentication (200 response)
  3) Missing authentication on POST /register — registration endpoint accessible without authentication (200 response)
  4) Missing authentication on POST /login — login endpoint accessible without authentication (200 response)
  5) Sensitive token leak on POST /login — access_token and token_type exposed in response to anon context
- Top fixes (max 5 bullets, max 1 line each):
  - Implement authentication middleware on all POST endpoints requiring authorization
  - Add role-based access control for patient record creation and management
  - Implement rate limiting on authentication endpoints to prevent brute force attacks
  - Remove sensitive token fields from responses or implement proper token handling
  - Add input validation and sanitization on all data creation endpoints

LOAD (scale_test)
- Route tested: POST /projects
- Status: FAILED
- Key metrics (single line):
  p95=510ms p99=1753ms failed=8.4% 5xx=0% 429/503=0% timeout=0%
- If 401 occurred: NO; Retry attempts used: 0; Bodies tried: {"title":"test","description":"test","category":"test","goal":1000,"progress":0,"location":"test"}
- Top bottleneck hypothesis (max 3 bullets, 1 line each):
  - Server crashed under load at 89 concurrent users with connection refused errors
  - Connection reset by peer errors indicate insufficient connection pool or worker processes
  - High p95/p99 latency (510ms/1753ms) suggests database or I/O blocking
- Top fixes (max 5 bullets, max 1 line each):
  - Increase worker processes or threads to handle concurrent connections
  - Implement connection pooling for database connections
  - Add health checks and graceful degradation under load
  - Implement request queuing and backpressure mechanisms
  - Add horizontal scaling capability with load balancer

LIMITATIONS (max 5 bullets)
- Load test was aborted early due to server crash at 89 VUs (target was 200 VUs)
- IDOR testing limited by lack of multi-user resource separation in responses
- Privilege escalation checks limited by lack of admin-specific routes identified
- Security scan could not test authenticated routes that require valid tokens
- Load test simulated only one endpoint type (POST /projects) not full production traffic mix
