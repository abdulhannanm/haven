HAVEN REPORT
Base URL: http://app:8000
Routes found: 21

SECURITY RUN SUMMARY
- Contexts attempted: anon,userA,userB,admin
- Contexts authenticated (non-0 status): anon,userA,userB,admin
- Checks executed: missing-auth=YES priv-esc=YES idor=YES sensitive-leak=YES
- Requests made (if known): 57
- Access matrix (route outcomes counts):
  anon_2xx=8 anon_401_403=0 anon_other=0
  userA_2xx=8 userA_401_403=0 userA_other=0
  userB_2xx=8 userB_401_403=0 userB_other=0
  admin_2xx=8 admin_401_403=0 admin_other=0

SECURITY (security_boundary_scan)
- Confidence: HIGH
- Findings: CRIT 0 | HIGH 6 | MED 0 | LOW 0
- Top 5 risks (max 5 bullets, max 1 line each):
  1) Missing authentication on POST /projects — anon can create projects (200)
  2) Missing authentication on POST /patients — anon can create patient records (200)
  3) Missing authentication on POST /register — anon can register new users, no rate limit observed
  4) Missing authentication on POST /login — anon can authenticate, observed on successful 200
  5) Sensitive data leak on POST /login — access_token exposed in response (anon=200)
- Top fixes (max 5 bullets, max 1 line each):
  - Require authentication for POST /projects, /patients, /donations, /volunteers
  - Rate-limit /register and /login to prevent brute-force and account enumeration
  - Remove or rotate exposed access_token values; consider opaque tokens or shorter TTL
  - Enforce role-based access control on sensitive data endpoints (projects, patients, donations)
  - Add CSRF protection for state-changing POST routes

LOAD (scale_test)
- Route tested: POST /login
- Status: FAILED
- Key metrics (single line):
  p95=372.76ms p99=null failed=0.00% 5xx=0.00% 429/503=0.00% timeout=0.00%
- If 401 occurred: NO; Retry attempts used: 0; Bodies tried: {"email":"sample","password":"sample"}
- Top bottleneck hypothesis (max 3 bullets, 1 line each):
  - p95 latency (372.76ms) exceeds threshold causing premature test abort despite 0% error rate
  - Database query or bcrypt password hashing under load likely causing elevated p90/p95 response times
  - No evidence of connection pooling limits, but threshold failure suggests tuning needed
- Top fixes (max 5 bullets, max 1 line each):
  - Optimize password hashing rounds (bcrypt cost factor) or switch to async workers
  - Add database connection pooling and optimize /login query indexes
  - Raise k6 p95 threshold to 500ms or use p99<1s for more realistic production tolerance
  - Cache user lookups or session tokens to reduce DB load on repeated logins
  - Implement rate limiting and CAPTCHA to prevent abuse at scale

LIMITATIONS (max 5 bullets)
- Only 2 routes load tested; broader coverage (GET /projects, /donations, /stats) recommended
- POST /donations failed all scale_test attempts (422/400 errors); unable to assess donation flow scalability
- IDOR testing limited by lack of discovered resource IDs from list routes in automated scan
- No privilege-escalation evidence beyond missing-auth; admin-specific routes not exercised under load
- Security scan deterministic contexts may not reflect real multi-tenant or org-scoped access patterns
